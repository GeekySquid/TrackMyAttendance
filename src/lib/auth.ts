/**
 * auth.ts — Clerk Auth helpers
 *
 * Authentication is now handled by Clerk (@clerk/clerk-react).
 * This file is kept for any shared auth utilities.
 * Use Clerk hooks directly in components:
 *   - useUser()    → current user + profile
 *   - useAuth()    → signOut, getToken, etc.
 *   - useSignIn()  → signIn.authenticateWithRedirect()
 *   - useSignUp()  → sign up flow
 */

// Re-export for convenience if needed
export {};
